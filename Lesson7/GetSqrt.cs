namespace Lesson7;

/// Create console application which would be able to calculate square roots for the numbers in range from M to N, where M and N – arguments what can be passed by the user. Print the pairs “number, square root” in the ascending order.
public class GetSqrt
{
    
    //CONSTRUCTOR FROM M , N
    public GetSqrt(int m, int n)
    {
        this.M = m;
        this.N = n;
    }
    
    
    
}